/**
 * React root entry for the extension UI (rendered inside shadow DOM).
 * This file is not used directly — the content script mounts the App component.
 * Exported for reference.
 */

export { App } from './App'
